name: Deploy LXC Containers

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Terraform action'
        required: true
        default: 'apply'
        type: choice
        options:
          - plan
          - apply
          - destroy

jobs:
  deploy-lxc:
    name: Deploy LXC Containers
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./instances/prod

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.0

      - name: Create terraform.tfvars (LXC only)
        run: |
          cat > terraform.tfvars <<'EOF'
          proxmox_api_url          = "${{ secrets.PROXMOX_API_URL }}"
          proxmox_api_token_id     = "${{ secrets.PROXMOX_API_TOKEN_ID }}"
          proxmox_api_token_secret = "${{ secrets.PROXMOX_API_TOKEN_SECRET }}"
          proxmox_tls_insecure     = true

          # LXC Containers configuration
          lxc_containers = ${{ vars.LXC_CONTAINERS }}

          # VMs configuration (empty for LXC-only deployment)
          vms = {}
          EOF

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        if: github.event.inputs.action == 'plan' || github.event.inputs.action == 'apply'
        run: terraform plan -target='module.lxc_containers'

      - name: Terraform Apply
        if: github.event.inputs.action == 'apply'
        run: terraform apply -target='module.lxc_containers' -auto-approve

      - name: Terraform Destroy
        if: github.event.inputs.action == 'destroy'
        run: terraform destroy -target='module.lxc_containers' -auto-approve
